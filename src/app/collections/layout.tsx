import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Collections",
  description:
    "Explore BHAGAT JI JEWELS — gold, diamond, bridal, temple & signature collections. Inquire for availability.",
};

export default function CollectionsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
